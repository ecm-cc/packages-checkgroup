{
  "name": "@ablegroup/checkgroup",
  "version": "1.0.5",
  "description": "Utility to check if a user is in a group",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Strutz",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^0.20.0"
  }
}
